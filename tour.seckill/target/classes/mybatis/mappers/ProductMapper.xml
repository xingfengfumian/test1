<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tour.seckill.mapper.ProductMapper" >
 	<!-- <insert id="insertProductList">
 	insert into product(product_id,city_id,title,price,property,reason) values
 	<foreach collection="#{productList}" item="item" separator=",">
 	(#{item.getProductId},#{item.getCityId},
 	#{item.getTitle},#{item.getPrice},
 	#{item.getProperty},#{item.getReason})
 	</foreach>
 	</insert> -->
</mapper>